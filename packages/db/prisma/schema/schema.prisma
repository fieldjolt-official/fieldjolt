generator client {
  provider   = "prisma-client-js"
  output     = "../generated"
  engineType = "client"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
