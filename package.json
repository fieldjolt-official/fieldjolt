{
  "name": "fieldjolt",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check --write .",
    "format": "biome format --write",
    "dev": "turbo dev",
    "build": "turbo build",
    "check-types": "turbo check-types",
    "dev:app": "turbo -F app dev",
    "dev:web": "turbo -F web dev",
    "dev:server": "turbo -F server dev",
    "db:push": "turbo -F @fieldjolt/db db:push",
    "db:studio": "turbo -F @fieldjolt/db db:studio",
    "db:generate": "turbo -F @fieldjolt/db db:generate",
    "db:migrate": "turbo -F @fieldjolt/db db:migrate",
    "setup-dev": "tsx scripts/setup-dev.ts",
    "check-setup": "tsx scripts/check-setup.ts",
    "new-branch": "tsx scripts/new-branch.ts",
    "reset-db": "tsx scripts/reset-db.ts",
    "seed-db": "cd packages/db && prisma db seed",

    "postinstall": "cd packages/db && prisma generate && cd ../.. && cd apps/docs && fumadocs-mdx"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "turbo": "catalog:",
    "ultracite": "catalog:"
  },
  "packageManager": "pnpm@10.19.0"
}
